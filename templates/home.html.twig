{% extends 'layouts/base.html.twig' %}

{% set style = asset('styles/home.scss') %}
{% set title = 'Parraindex' %}

{% block content %}

    <img class="logo" src="{{ asset('images/icons/logo-blue.svg') }}" alt="Logo">

    <a class="title" href="{{ url('tree') }}">
        <h1 class="title__text"><span class="title__prefix">Le</span> Parraindex</h1>
        <p class="title__subtext">
            L'annuaire des parrains de l'IUT Lyon 1
        </p>
    </a>

    {% include 'templates/navbar.html.twig' %}

{% endblock %}
