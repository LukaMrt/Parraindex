<div class="person-family">

	<p class="person-family__title">Parrains</p>

	<div class="person-family__wrapper">

		{% for godFather in godFathers %}
			<div class="person-family__portrait person-family__portrait--godfather">
				<img src="{{ picture(godFather.picture) }}" alt="{{ godFather.firstName }}" class="person-family__portrait-image">
				<p class="person-family__portrait-name">{{ godFather.firstName }} <span class="person-family__portrait-name--darker">{{ godFather.lastName }}</span></p>
			</div>
		{% endfor %}

		<div class="person-family__portrait">
			<img src="{{ picture(person.picture) }}" alt="{{ person.firstName }}" class="person-family__portrait-image">
			<p class="person-family__portrait-name">{{ person.firstName }} <span class="person-family__portrait-name--darker">{{ person.lastName }}</span></p>
		</div>

		{% for godChild in godChildren %}
			<div class="person-family__portrait person-family__portrait--godchild">
				<img src="{{ picture(godChild.picture) }}" alt="{{ godChild.firstName }}" class="person-family__portrait-image">
				<p class="person-family__portrait-name">{{ godChild.firstName }} <span class="person-family__portrait-name--darker">{{ godChild.lastName }}</span></p>
			</div>
		{% endfor %}
	</div>

	<p class="person-family__title person-family__title--second">Fillots</p>

</div>