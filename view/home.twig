{% extends '/view/layouts/default.twig' %}

{% set style = style('home.css') %}
{% set title = 'Parraindex - Accueil' %}

{% block content %}

	<h1>{{title}}</h1>

	{% include 'components/button.twig' with {
		'button_content' : 'first custom button',
		'priority' : 'primary',
		}
	%}

	{% include 'components/button.twig' with {
		'button_content' : 'second custom button',
		'priority' : 'secondary',
		}
	%}

{% endblock %}